# docs/

Add `demo.gif` here — a screen recording of `email-triage process --dry-run`
showing the classification table output in the terminal.

Suggested tool: [Gifox](https://gifox.app/) or `ffmpeg` → `gifsicle`.
